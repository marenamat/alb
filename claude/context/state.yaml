date: 2026-04-04
workflow_step: feature_branch_in_progress

maintenance:
  status: done
  notes:
    - Fetched github/main; 2 new commits (9b585a5 fix workflow, 9cd8dfb answers)
    - Rebased local main onto github/main (fast-forward)
    - Only one branch (main) exists + new feature branch feature/static-site-gen

github_pipelines:
  status: done
  notes:
    - No GitHub Actions workflows exist (total_count = 0)
    - Nothing to check

github_issues:
  status: done
  notes:
    - No open issues found via curl

design_check:
  status: done
  design_file: claude/design/base.md
  answers_applied:
    - access_control: ignore for v1; unlist albums without 'public' in access list
    - bootstrap: selfhosted v5.x, downloaded to static/vendor/
    - gimp: watch for new files with suffixes, auto-mark original hidden
    - landing_page: ~/foto dir, albums.yaml aggregated from subdirs, make install recursive
  new_rules:
    - All updates go to work branches (never touch main)
    - Use curl instead of gh

current_branch: feature/static-site-gen

features_implemented:
  - DELETE key to mark/unmark photos for omission (toggle_delete WebSocket command)
  - 5-minute auto-backup of index.yaml
  - img click selects, dblclick navigates
  - Deleted photos shown with red outline + low opacity
  - Escape on single view returns to main view
  - Bootstrap 5.3.3 vendored to static/vendor/
  - Static site generator (Alb/Generator.py)
  - generate CLI command (Alb/Command/Generate.py)
  - Generate button + WebSocket command in GUI
  - album-index.html.j2 + album-single.html.j2 templates (Bootstrap + Garamond)
  - Makefile template (Makefile.j2)
  - Landing page generator (Alb/Command/Landing.py)
  - landing.html.j2 template
  - GIMP file watcher (watches for <basename>-*.ext, marks original hidden)
  - Fixed Resizer quality bug (0-1 float -> 0-100 int for ImageMagick)

features_missing:
  - Album metadata editing in GUI: URL, thumbnail, access capabilities
  - ENTER key to open photo description editor
  - Shift+G to open GIMP (currently Ctrl+G in single view)
  - Browse-before-modification view in GUI

packages_needed:
  - py3-yaml
  - py3-jinja2
  - py3-aiohttp
  - Asked in claude/questions.md

questions_file: claude/questions.md
questions_pending:
  - Packages: py3-yaml, py3-jinja2, py3-aiohttp need to be installed (apk add)
