date: 2026-04-05
workflow_step: complete

maintenance:
  status: done
  notes:
    - github/main in sync with local main (no new commits)
    - no merged branches to prune

github_pipelines:
  status: skipped
  notes:
    - no Github actions configured for this project (confirmed in design/base.md)

github_issues:
  status: done
  notes:
    - issue #12 (new): "Viewing improvements" — fixed in fix/12-viewing-improvements
      changes:
        - dark/light mode via data-bs-theme + inline prefers-color-scheme JS (no FOUC)
        - all three generated templates updated: album-single, album-index, landing
        - replaced hardcoded #555/#ccc/#888 with Bootstrap CSS vars (--bs-secondary-color, --bs-border-color)
        - fixed text-dark -> text-body on landing page for dark mode compat
        - Esc -> thumbnails was already implemented in album-single.html.j2
    - issue #11 (open): fix/11-memory-oom — OOM fix for large album generation
    - issue #10 (open): fix/10-shift-tab — shift-tab navigation
    - issue #9 (open): fix/9-exif-display — EXIF display
    - issue #8 (open): fix/8-gimp-editing — GIMP mod tracking

design_check:
  status: done
  notes:
    - No design file changes

branches:
  - main: in sync with github/main
  - fix/8-gimp-editing: GIMP mod tracking, originals side panel, WebSocket broadcast
  - fix/9-exif-display: EXIF display in single-photo GUI (stacked on fix/8)
  - fix/10-shift-tab: shift-tab navigation (stacked on fix/9)
  - fix/11-memory-oom: OOM fix for large album generation (standalone on main)
  - fix/12-viewing-improvements: dark/light mode respecting OS setting (standalone on main)
  - feature/migrate-tool: adds 'alb migrate' command (standalone on main)
