recorded_at: '2026-04-13'
branch: issue-18
status: done
issue: 18
summary: >
  Fixed issue #18: clanker-prep-generated context files (git_status.yaml,
  github_issues.yaml, pipeline_results.yaml, seen_*.yaml) are now gitignored
  and removed from tracking. They were always regenerated by clanker-prep each
  run, causing spurious dirty-workdir triggers. The PREP_OUTPUTS exclusion in
  clanker-prep remains for safety but is no longer the primary guard.
notes:
  - Issue #15 branch exists with housekeeping feature but rebase_failed due to
    context file conflicts; those conflicts are now resolved by the gitignore fix.
  - Issue #15 needs to be rebased onto main after this fix is merged.
